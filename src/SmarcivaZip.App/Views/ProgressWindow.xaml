<Window x:Class="SmarcivaZip.App.Views.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="smarcivaZIP"
        Icon="/Assets/smarcivazip.ico"
        Width="460" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True">
  <Grid Margin="20,18,20,16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock x:Name="HeadingText" Grid.Row="0" Style="{StaticResource Heading}" Text="解凍しています" />

    <TextBlock x:Name="DetailText" Grid.Row="1" Style="{StaticResource Caption}"
               Margin="0,6,0,0" TextTrimming="CharacterEllipsis" Text="準備中..." />

    <Grid Grid.Row="2" Margin="0,14,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <ProgressBar x:Name="Bar" Grid.Column="0" VerticalAlignment="Center" Minimum="0" Maximum="1" />
      <TextBlock x:Name="PercentText" Grid.Column="1" Margin="12,0,0,0"
                 Style="{StaticResource Caption}" VerticalAlignment="Center"
                 MinWidth="40" TextAlignment="Right" Text="0%" />
    </Grid>

    <Button x:Name="CancelButton" Grid.Row="3" Content="キャンセル"
            HorizontalAlignment="Right" Margin="0,18,0,0" Click="OnCancelClicked" />
  </Grid>
</Window>
